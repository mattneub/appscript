osadict Read Me

To install osadict, cd to the Extras/osadict directory in Terminal.app and run:

	sudo cp osadict /usr/local/bin
	sudo chmod 755 /usr/local/bin/osadict

To view osadict's help, run 'osadict' (or '/usr/local/bin/osadict').